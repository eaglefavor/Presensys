feat: add animated SplashLogo to PWA

This commit introduces a new custom `SplashLogo` component matching the requested "Seamless Arrival" Concept 1 for the PresenSys app.

- Uses SVG and CSS Keyframes to drive a 2.5s loop animation.
- Implements the "draw on" effect for the gray track.
- Implements the "fill" and "morph to checkmark" with colors transitioning from Synergy Blue (`var(--bs-primary)`) to Arrival Green (`var(--bs-success)`).
- Typography correctly matches the design with "Presen" holding steady and "Sys" recoloring.
- Integrated `SplashLogo` into the `PageLoader` inside `src/App.tsx`, completely replacing the old unbranded Bootstrap spinner layout while the application initiates.
